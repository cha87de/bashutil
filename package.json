{
  "name": "bashutil",
  "version": "0.0.1",
  "description": "utility functions like log, retry, trim, ...",
  "install": "make install",
  "repo": "cha87de/bashutil",
  "global": true,
  "scripts": ["log", "error", "trim", "retry"]
}
